stack {
  name        = "storage"
  description = "Evidence pack 共用 GCS bucket（SARIF / Asset Inventory / Audit Logs 落地點）。設計見 research/audit/decisions/ADR-003。"
  id          = "evidence-storage"
  tags        = ["evidence"]
}
